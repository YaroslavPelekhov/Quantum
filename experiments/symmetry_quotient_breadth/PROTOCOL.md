# Frozen QOBLIB quotient breadth protocol

Frozen after the read-only twin census and before any new state evolution:
2026-08-28.

Run every non-aves case in the pre-existing deterministic QOBLIB cohort
selection.  No case may be removed based on twin compression.  Use the original
reduction cap, MIS penalty 1.5, depth 15, and the first two already frozen
schedule genomes from the symmetry experiment.

For each case, compare quotient and dense exact states over the full basis and
compare the quotient and dense DCS spectrum at `floor(n/2)` for the exact
maximum-independent-set event.

All seven rows must have maximum amplitude error below `1e-11`, identical DCS
numerical rank, trace and trace-norm errors below `1e-10`, and norm error below
`1e-10`.  Cases without twins are required to run as explicit no-compression
controls.  Runtime and compression are descriptive.

